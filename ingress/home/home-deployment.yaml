apiVersion: apps/v1 
kind: Deployment 
metadata: 
  name: home-deployment 
  labels: 
    app: home 
spec: 
  replicas: 2
  strategy:
    type: RollingUpdate 
    rollingUpdate: 
      maxSurge: 1
      maxUnavailable: 0
  selector: 
    matchLabels: 
      app: home
  template: 
    metadata: 
      labels: 
        app: home 
    spec: 
      containers: 
        - name: apache-container
          image: httpd:latest
          ports: 
            - containerPort: 80
          
          resources: 
            requests: 
              cpu: "100m"
              memory: "64Mi"
            limits: 
              cpu: "200m"
              memory: "128Mi"